import React, { Component } from 'react';
import { Section } from '../index.styles';

const Todo = () => {
  return (
    <Section id="todo">
      <Title>What to do?</Title>
    </Section>
  );
};

export default Todo;
